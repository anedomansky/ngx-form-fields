import { AsyncValidatorsDirective } from './async-validators.directive';

describe('AsyncValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
